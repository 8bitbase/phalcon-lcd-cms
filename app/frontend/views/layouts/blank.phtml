<!doctype html>
<html>
    {{ partial('partials/head') }}
    {{ partial('partials/css') }}
    <body>
        <!--Start main-content -->
        {{ content() }}
        <!--End main-content -->
    </body>
</html>